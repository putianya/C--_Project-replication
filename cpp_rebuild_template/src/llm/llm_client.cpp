#include "llm/llm_client.h"

// llm_client.h 中只有纯虚接口，无需额外实现。
// 本文件保留为占位，供后续添加工厂方法或公共工具函数。

namespace llm {
// 工厂示例（取消注释后使用）:
// std::unique_ptr<LLMClient> createOpenAIClient(const std::string& apiKey);
} // namespace llm
